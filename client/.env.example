# Azure OpenAI configuration for intent detection
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_MODEL=gpt-4o-mini
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# A2A agent URL (optional, defaults to http://localhost:8000)
A2A_AGENT_URL=http://localhost:8000

# Entra ID app IDs for EasyAuth-protected Azure endpoint
# API app ID is required for cloud auth; leave unset for local-only testing
# Client app ID is recommended (public client app used by this CLI)
# Value of this variable is the same as ENTRA_API_APP_CLIENT_ID in .azure/<environment>/.env
# A2A_AGENT_API_APP_ID=
# Value of this variable is the same as ENTRA_CLIENT_APP_CLIENT_ID in .azure/<environment>/.env
# A2A_AGENT_CLIENT_APP_ID=
# Backward compatibility: A2A_AGENT_CLIENT_ID can be used as API app ID
# AZURE_TENANT_ID=
