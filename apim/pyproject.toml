[project]
name = "apim-mcp-setup"
version = "0.1.0"
description = "Python environment for the APIM MCP setup notebook"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=6.29",
    "mcp>=1.0.0",
    "nest-asyncio>=1.6",
]

